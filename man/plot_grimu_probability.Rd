% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_grimu_probability.R
\name{plot_grimu_probability}
\alias{plot_grimu_probability}
\title{Plot GRIMU Probability Space for Multiple Sample Size Pairs}
\usage{
plot_grimu_probability(n1_vec, n2_vec)
}
\arguments{
\item{n1_vec}{A numeric vector of sample sizes for group 1.}

\item{n2_vec}{A numeric vector of sample sizes for group 2. Must be the
same length as \code{n1_vec}.}
}
\value{
A \code{ggplot} object displaying a faceted grid of p-value probability
spaces. Black tiles indicate mathematically possible p-values (rounded up
to 3 decimals), while gray tiles indicate impossible values.
}
\description{
Visualizes the "granularity" of p-values for the Mann-Whitney U test across
multiple sample size combinations. The function generates a heat-map style
grid where each tile represents a possible 3-decimal p-value. This is useful
for meta-scientific screening to identify impossible reported p-values.
}
\details{
The function uses \code{grimu_map_pvalues} to calculate exact p-values for all
possible U statistics given \code{n1} and \code{n2}. It then maps these onto a 10x100
grid where the y-axis represents the first decimal place and the x-axis
represents the second and third decimal places.

Note that this function assumes the absence of ties and uses exact
probability calculations.
}
\examples{
\dontrun{
plot_grimu_probability(n1_vec = c(8, 30), n2_vec = c(9, 31))
}
}
